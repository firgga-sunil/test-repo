apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}
spec:
  type: {{ .Values.service.type | default "ClusterIP" }}
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: {{ .Values.name }}
